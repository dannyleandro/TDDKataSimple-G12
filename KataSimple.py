class Estadistica:

    def getArreglo(self, cadena):
        # length = len(cadena)
        #cadena = [length]
        if cadena == '':
            return [0]
        else:
            return [int(cadena)]
